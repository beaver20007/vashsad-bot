# Аудит 152-ФЗ — vashsad-bot (read-only, без правок кода)

Дата: 2026-08-25. Метод: grep/чтение кода репозитория `C:\Projects\vashsad-full`
(bot.py, admin_bot.py, handlers/, services/). Инфраструктурные факты (регион БД,
реестр РКН) кодом не проверяются — помечено отдельно. Тот же повод/список 10
пунктов, что в параллельном аудите `vashsad-miniapp` (`docs/audit/152fz-audit-20260825.md`
в этом репо), фокус — на боте.

---

**1. Политика обработки ПДн**
Файла/текста политики в репозитории нет. `Glob **/*privacy*` → 0 совпадений;
grep `конфиденциальност|политика.*обработ` по `*.py` → 0 совпадений.
→ **Факт: отсутствует.**

**2. Согласие на обработку ПДн**
Grep `согласи|consent|соглаша` по всем `*.py` → 0 совпадений (на момент аудита).
Первое сообщение `/start` (handlers/start.py:130-225) — без чекбокса/текста
согласия. Все FSM-формы с контактными данными — handlers/booking.py:123
(телефон), handlers/plan.py (бриф без телефона/email) — тоже без согласия.
→ **Факт (на момент аудита): отсутствует, в том числе в /start.**
→ **Статус: закрыто треками fix/t-bot-consent-start (PR #19) и
fix/t-bot-consent-booking (PR #20), см. docs/ORCHESTRATOR.md, запись
2026-08-26.**

**3. Уведомление РКН**
Административный факт, не проверяется кодом.
→ **Не могу подтвердить/опровергнуть — нужна проверка реестра операторов РКН
владельцем.**

**4. Трансграничная передача — ПРИОРИТЕТНЫЙ ПУНКТ**
- `users` (telegram_id, username, first_name, region) — пишет `get_or_create_user()`.
- `bookings.phone` — пишет **сам бот напрямую**, handlers/booking.py:123-136,
  отдельно от миниаппа.
- `orders.phone/email` — проверено: пишет только миниапп, бот (handlers/plan.py)
  передаёт только area/style/wishes.
- **Neon (Postgres)** — все таблицы выше.
- **Redis/Upstash** — FSM-хранилище (bot.py, RedisStorage), не просто кеш;
  телефон в него не попадает (пишется в Postgres напрямую из booking.py).
- **Resend (email, api.resend.com, США)** — функции с полями phone/email
  определены (services/email_service.py), но нигде не вызываются в коде бота.
- **smsc.ru (SMS, РФ)** — аналогично, определены, не вызываются.
- **Sentry (sentry.io)** — `send_default_pii` не установлен (default False), но
  SDK по умолчанию прикладывает локальные переменные к трейсбеку
  (`include_local_variables=True` по умолчанию) — потенциальный канал утечки
  `phone` при падении booking.py.
→ **Факт: бот пишет `bookings.phone` напрямую, отдельно от миниаппа.
Email/SMS с ПДн определены, но мертвы. Sentry — конфигурационный риск.**
→ **Статус: Sentry-риск закрыт треком fix/t-sentry-scrub-locals (PR #21),
см. docs/ORCHESTRATOR.md, запись 2026-08-26.**

**5. Локализация первичного сбора (ст. 18)**
Бот пишет `bookings.phone` первым в Neon — своей копии у бота нет, это
первичная запись. Физический регион Neon-инстанса кодом не читается.
→ **Факт для юриста: требует проверки в панели Neon/Railway.**

**6. Cookie/аналитика**
Grep `google-analytics|gtag|GA_TRACKING|Яндекс.Метрика` → 0 совпадений. У бота
нет веб-страниц/cookies. Есть собственная таблица `analytics_events`
(остаётся в той же Neon-БД, третьей стороне не передаётся).
→ **Факт (проверено, не по умолчанию): N/A.**

**7. Ответственность оператора**
Использование Neon/Upstash/Resend/smsc.ru/Railway не снимает ответственности
оператора ПДн с Beaver.

**8. Платёжные данные**
YooKassa: redirect на хостинг-страницу, в код бота передаются только
amount/description/telegram_id/plan. Telegram Stars — оплата внутри Telegram.
Карточные данные через код не проходят.
→ **Факт: N/A для текущей схемы, проверить повторно перед нехостированной
оплатой.**

**9. Срок хранения / удаление ПДн по запросу**
Механизма удаления (даже ручного/админского) в коде бота нет. `/export` —
выгрузка всех заявок **для дизайнера** (CSV), не self-service удаление.
→ **Факт: отсутствует.**

**10. Итоговая пометка**
Пункты 3, 5, 7 требуют юридического толкования/инфраструктурной проверки —
не додуманы, помечены отдельно.

---

**Статус закрытия находок (обновляется по мере трекинга):**
- Пункт 2 (бот) — закрыт PR #19 (общее согласие в /start + глобальный гейт)
  и PR #20 (отдельное согласие перед вводом телефона в booking.py).
- Пункт 4 (Sentry-риск) — закрыт PR #21.
- Остальное — без изменений, требует юриста/административных действий
  владельца или относится к миниаппу (отдельный трек).
