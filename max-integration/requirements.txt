# MAX Bot API — официальная библиотека
# ВАЖНО: версия зафиксирована — MAX API нестабильно, обновлять осознанно
max-botapi>=0.1.0

# HTTP клиент для прямых вызовов API
httpx>=0.27.0

# FastAPI для webhook endpoint (уже есть в основных проектах)
fastapi>=0.115.0
uvicorn>=0.30.0

# Логирование
loguru>=0.7.0

# ENV
python-dotenv>=1.0.0
